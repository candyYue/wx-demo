<!--index.wxml-->
<view id="container" >
    <view class="tab-list">
      <view bindtap="dateDetail"
      wx:for="{{List}}" wx:key="index" data-label="{{item.label}}" data-index="{{index}}">
      <text>{{item.value}}</text>
      </view>
    </view>
</view>
